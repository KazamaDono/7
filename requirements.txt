# Core dependencies
# NOTE: exact pins below broke on Python 3.14 -- edge-tts 6.1.9 has the TTS 403
# bug and Pillow 10.4.0 fails to compile. Use floors so pip picks a build that
# works on this interpreter instead of forcing a broken one.
edge-tts>=7.0.0
pygame==2.6.1
# SpeechRecognition 3.10.x imports the `aifc` stdlib module, which Python 3.13
# REMOVED -- so a fresh install on 3.13/3.14 crashes on `import aifc`. 3.14+ of
# SpeechRecognition works with the backports below.
SpeechRecognition>=3.14
pyaudio==0.2.14

# Backports of stdlib modules removed in Python 3.13, needed by SpeechRecognition.
# Only installed on 3.13+ (harmless/absent on older Pythons).
standard-aifc; python_version >= "3.13"
audioop-lts; python_version >= "3.13"

# LLM and AI
langchain==0.3.13
langchain-ollama==0.2.2
langchain-core==0.3.28

# System and utilities
# psutil powers the agent's system-inspection tools (top_processes, system_status).
psutil>=5.9.8
requests==2.32.3
python-dotenv==1.0.1
schedule==1.2.2
keyboard==0.13.5

# Image processing (for orb)
Pillow>=11.0.0

# Note: the hologram/ hand-gesture module runs in its own Python 3.11 venv
# (hologram/.venv) with its own requirements.txt -- see hologram/requirements.txt.
# It is NOT installed here because legacy mediapipe.solutions has no compiled
# build for newer Python versions.

# Optional but recommended
colorama==0.4.6
rich==13.9.4


pygame>=2.5.0
SpeechRecognition>=3.14
edge-tts>=7.0.0
langchain-ollama>=0.1.0
pillow>=11.0.0
pyaudio>=0.2.11
psutil>=5.9.0
requests>=2.31.0
python-dotenv>=1.0.0
schedule>=1.2.0
pyinstaller>=6.0.0